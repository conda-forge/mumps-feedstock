mpi:
  - nompi
  - mpich  # [not win]
  - openmpi  # [not win]

pin_run_as_build:
  parmetis:
    min_pin: x.x.x
    max_pin: x.x
  scalapack:
    min_pin: x.x.x
    max_pin: x.x


fortran_compiler:           # [win]
  - flang                   # [win]
fortran_compiler_version:   # [win]
  - 19                      # [win]

channel_sources:                            # [win]
  - conda-forge/label/llvm_rc,conda-forge   # [win]
