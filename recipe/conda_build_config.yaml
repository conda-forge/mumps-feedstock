mpi:
  - nompi
  - mpich  # [not win]
  - openmpi  # [not win]
blas_impl:
  - openblas  # [not win]
  - mkl  # [linux64]
  - netlib

pin_run_as_build:
  parmetis:
    min_pin: x.x.x
    max_pin: x.x
  scalapack:
    min_pin: x.x.x
    max_pin: x.x
