mpi:
  - nompi
  - mpich  # [not win]
  - openmpi  # [not win]
blas_impl:
  - openblas  # [not win]
  - netlib

pin_run_as_build:
  parmetis:
    min_pin: x.x.x
    max_pin: x.x
  scalapack:
    min_pin: x.x.x
    max_pin: x.x
